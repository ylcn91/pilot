# Pilot Full Audit — 2026-05-25

**Snapshot:** branch `main` @ `fd0f7b69` (v2.149.3) · 28 internal packages · 11 adapters · ~30k LOC Go
**Method:** 7 parallel research agents (exhaustive depth, file:line citations, all-severity), each fed a self-contained brief. One stream (defensive code review) was dropped after two policy-filter blocks; its surface area is partially covered by Streams 3/5/6.
**Output:** This report — single source. No issues/tasks auto-created.

---

## Table of contents

- [0. Executive summary](#0-executive-summary)
- [1. Cross-stream convergent findings](#1-cross-stream-convergent-findings) ← read this first
- [2. Top 10 recommended actions](#2-top-10-recommended-actions)
- [3. Findings by stream](#3-findings-by-stream)
  - [3.1 Architecture & boundaries](#31-architecture--boundaries)
  - [3.2 Code quality & tech debt](#32-code-quality--tech-debt)
  - [3.3 Test coverage & quality](#33-test-coverage--quality)
  - [3.4 Observability & error handling](#34-observability--error-handling)
  - [3.5 Regression pattern analysis](#35-regression-pattern-analysis)
  - [3.6 Documentation drift](#36-documentation-drift)
  - [3.7 Config & operational risk](#37-config--operational-risk)
- [4. Recurring themes](#4-recurring-themes)
- [5. Skipped scope and limitations](#5-skipped-scope-and-limitations)
- [Appendix A: Architecture map](#appendix-a-architecture-map)
- [Appendix B: Fix-flux table](#appendix-b-fix-flux-table)
- [Appendix C: Env var registry](#appendix-c-env-var-registry)
- [Appendix D: Coverage map](#appendix-d-coverage-map)

---

## 0. Executive summary

Pilot is shipping working software at high cadence (v2.149.x = 4 patches in ~24h) but the audit surfaces three systemic issues underpinning most of the recent incidents:

1. **No single source of truth for "task completed."** Six call sites compute the same predicate against different fields (`executions.status`, `commit_sha`, `pr_url`, `pilot-done` label, PR `merged`, `isParentDone`). v2.149.3 hardened the post-flight notifier; TASK-288 documents that the pre-dispatch poller predicate still allows the same false positives through. This is the #1 recurring bug class and is wired into the dispatch-loss class that hit GitNation on 2026-05-21.
2. **God objects + scanner-gate ordering as accident-prone surface.** `internal/executor/runner.go` is 4,556 lines with 111 methods on one `Runner` struct. `cmd/pilot/main.go` has a 1,400-line `runPollingMode`. `internal/autopilot/controller.go` has 5+ handler functions over 100 lines. Multi-gate scanners (`ScanRecentlyMergedPRs`, `checkForNewIssues`) grew gates incrementally with no phase discipline — every observability/metric commit risks landing on the wrong side of a skip gate (see `b140e441`, `6df99408`).
3. **Silent skip is the default operational mode.** Guards return `continue`/`return` with at most a Debug log; multiple multi-hour outages (workshop demo, GH-2301, 2026-04-17) required source-code reading to root-cause. There is no metric for poller skip-by-reason, no counter for repo-allowlist bypass, no panic-recovery wrapper on any production goroutine, and `Metrics.RecordAPIError` is dead code (`pilot_api_errors_total` always emits zero).

Secondary themes:
- **Documentation drift is severe.** CLAUDE.md still claims `v2.53.0 | 316 features` (96 minors stale); FEATURE-MATRIX frozen at v2.53.0; ARCHITECTURE.md frozen at v2.56.0; `--autopilot=` documented as primary CLI flag in 4 places but it's a hidden alias for `--env=`. At least two Navigator pitfall memories cite live code locations for fixes that already shipped.
- **Default-off operational hardening.** Smart retry, stagnation detection, subprocess RSS caps, alerts engine, autopilot itself — all default to disabled. Newly-onboarded users get the un-hardened experience by default.
- **Adapter polling is triplicated.** GitHub/GitLab/Azure DevOps pollers share ~90% structure (~3,000+ LOC of near-duplication); a fix to one doesn't propagate.
- **Test surface is enumerative, not structural.** Tests reproduce the exact reported scenario (good) but rarely assert invariants across all call sites (bad). The repeat GH-201 fixes are the canonical example: 4 negative cases added across 2 PRs, none covering the round-trip path.

**Findings: 178 total**
- P1: 28 — critical (active prod risk, regression magnet, silent failure mode)
- P2: 79 — significant (fragile pattern, missing guard, complexity hotspot)
- P3: 51 — refactor candidate / tech debt
- P4: 20 — nits, naming, comment hygiene

---

## 1. Cross-stream convergent findings

The findings below were independently surfaced by 2+ research agents using different lenses. High-confidence, high-leverage.

### CS-1. ★ "Completed" predicate inconsistency across 6+ sites — `[P1]`
- Surfaced by: Stream 1 (P1), Stream 5 (P2 swallowed-error variant), Stream 6 (#1 recurring bug class)
- Files: `internal/memory/store.go:540-551`, `internal/adapters/github/poller.go:1022-1037`, `cmd/pilot/handlers.go`, `cmd/pilot/commands.go:1165`, `internal/autopilot/controller.go`, `internal/executor/dispatcher.go:208,249`
- Root: No `executor.IsTaskShipped(row)` predicate; each site composes SQL/label/state differently. v2.149.3 hardened the post-flight notifier but did NOT update `HasCompletedExecution` symmetrically. Per TASK-288, the same false-positive rows that the notifier now rejects still trigger silent dispatch skip on every 30s poll tick.
- Impact: Cleanly-installed v2.149.3 can silently stop dispatching after a single legacy false-complete row. The 2026-05-21 GH-21/22/26 incident lost dispatch for ~5h.
- Fix: Extract `func IsTaskShipped(row Execution) bool` requiring `(commit_sha != "" OR pr_url != "")` AND status=completed; route both the post-flight notifier (cmd/pilot) and pre-dispatch poller (memory/store.go) through it; add a cross-site invariant test asserting both decisions agree on the same row.

### CS-2. ★ Closed-parent / stale-state dispatch loop — `[P1]`
- Surfaced by: Stream 6 (#2 recurring bug class, OAuth cascade incident), Stream 1 (P3 `testing` import in epic.go reflects ad-hoc gating)
- Files: `internal/executor/epic.go` (12 fix commits in last 150), `internal/executor/dispatcher.go:639`, `cmd/pilot/handlers.go:232`
- Recent commits: `d659d73e` (GH-201 fix #1), `59c1d252` (residual hole), `0f1829a8`, `30f91ec9`, Apr 2026 OAuth cascade (`891affe3..424b0198` — 10 revert commits, 64 duplicate sub-issues, 19h)
- Root: `Task` is a snapshot of GitHub state at queue time; dispatcher reconstructs from `executions` rows that don't carry `task_state`. Every entry point must call `isParentDone` defensively, and several have forgotten.
- Fix: Add `task_state` column to `executions`; refresh from live on dispatcher reconstruction; centralize gate behind `MustParentBeActionable(task)` chokepoint with build-tag enforcement.

### CS-3. ★ Production goroutines have zero panic recovery — `[P1]`
- Surfaced by: Stream 5 (P1), Stream 2 (related: `goroutine leaks (go func() without cleanup)`)
- Files: `internal/autopilot/controller.go:1700`, `internal/executor/parallel.go:129`, `internal/executor/runner.go:2892,2902`, `internal/webhooks/manager.go:78`, `internal/executor/dispatcher.go:437`, `internal/adapters/github/poller.go:1127`, `internal/gateway/server.go:247`, `internal/executor/backend_claudecode.go:459,506,544,608,621`, `internal/executor/backend_qwencode.go:284,328,362,409,422`, `internal/executor/rss_sampler.go:20`, `internal/approval/manager.go` (cancel goroutine)
- Issue: 0 `recover()` calls in production. A nil-deref in any adapter callback, RSS sampler, or worker brings the whole daemon down.
- Impact: Any panic in a subordinate subsystem crashes the binary and loses every in-flight task plus the alerting pipeline.
- Fix: Add `safeGo(fn)` helper in `internal/logging` that defers `recover()`, logs with stack at Error, increments `pilot_panics_total{component}`; replace all production `go func(){…}()`.

### CS-4. ★ Silent skip is the default operational mode — `[P1]`
- Surfaced by: Stream 5 (P1 poller skip-by-reason), Stream 6 (silent skip/disable bug class), Stream 7 (`--autopilot` flag drift = silent deprecation)
- Files: `internal/adapters/github/poller.go:919-1040` (9 skip branches, none counted), `internal/executor/repo_guardrail.go:81-94` (bypass logs Warn only, no metric/alert), `cmd/pilot/main.go:1406,2265` (autopilot silently disabled until `f032689b`)
- Root: New gates default to silent `continue`. No convention, no lint, no Prometheus counter for "skipped because X". Operations debugging cost is high.
- Fix: Add `pilot_poller_skipped_total{repo, reason}` + `pilot_poller_dispatched_total{repo}` counters; add `pilot_repo_guardrail_bypass_total{owner, repo}` counter + dedicated `AlertTypeRepoGuardrailBypass` rule; introduce `logSkip(reason, fields)` helper + grep CI check that flags `continue` inside polling loops without preceding log call.

### CS-5. ★ Destructive git operations + half-finished SOP — `[P1]`
- Surfaced by: Stream 6 (P1), Stream 1 (no architectural callout but confirms the file)
- Files: `internal/executor/worktree.go:349` still does `reset --hard` with "safe because tmpdir" comment; SOP at `.agent/sops/git/never-reset-hard-in-automated-flows.md`; v2.146.7 fixed `syncMainBranch` only
- Issue: SOP says "never `reset --hard` in automated flows"; worktree.go violates it with the exact reasoning that was wrong for `syncMainBranch` (two PRs lost on the demo machine before that was caught).
- Fix: Audit every `reset --hard` and `git push --force`; replace with `--ff-only` + explicit divergence handling, OR add a `if !strings.HasPrefix(path, os.TempDir()) { panic(...) }` precondition before destructive ops.

### CS-6. Adapter-level coupling and triplication of polling logic — `[P2]`
- Surfaced by: Stream 1 (P2 `autopilot` imports concrete adapters; P2 adapter→executor coupling), Stream 2 (P2 3 issue pollers with 90% identical structure; P2 7 per-adapter `*_processed` tables)
- Files: `internal/adapters/{github,gitlab,azuredevops}/poller.go` (~3,400 LOC near-duplicate); `internal/adapters/{github,gitlab,azuredevops}/cleanup.go`; `internal/autopilot/state_store.go:65-114` (7 `*_processed` tables); `internal/autopilot/{feedback_loop,telegram_notifier,scope_guard,releaser,auto_merger,ci_monitor,controller,deployer,release_summary}.go` all import concrete `github`/`telegram`
- Root: `autopilot` is the policy layer but holds `*github.Client` and `*telegram.Client` as concrete fields. Each new adapter triplicates ~1000 LOC of poller code and ~100 LOC of state-store boilerplate.
- Fix: Extract generic `Poller[T any]` with `SourceAdapter` interface; consolidate 7 `*_processed` tables into existing `adapter_processed` generic table; convert autopilot's concrete adapter imports to interface-based dependencies (the `Notifier` interface exists at `controller.go:65` — finish the job for `GitHubOperator`).

### CS-7. God objects in executor + cmd layer — `[P2]`
- Surfaced by: Stream 1 (P2 runner.go 4556 lines; P3 cmd/pilot god functions; P3 pilot.go 1500 lines), Stream 2 (Hotspot #1 runner.go single 2,203-line function)
- Files: `internal/executor/runner.go` (4,556 lines, 111 methods, `executeWithOptions` is 2,203 lines), `cmd/pilot/main.go` (2,988 lines, `runPollingMode` ~1,400 lines), `cmd/pilot/commands.go` (2,968 lines), `internal/pilot/pilot.go` (1,500 lines, 43 methods, 35 fields), `internal/dashboard/tui.go` (2,842), `internal/autopilot/controller.go` (2,532)
- Impact: Every test for any executor feature loads the full Runner. PRs touching runner.go produce massive diffs that are hard to review. cmd/pilot wiring is untestable.
- Fix: Extract `PromptBuilder` (partly done), `ProgressTracker`, `RetryOrchestrator`, `MetricsFlusher`, `StageHandler` from controller.go; introduce `internal/startup/` to hold `runPollingMode` body and its three shim types (`storeTaskChecker`, `preFlightJudgeShim`, `storeExecutionSaver`); decompose `Pilot` god struct into per-adapter `Manager` types.

### CS-8. Webhook signature verification gap on 2 adapters — `[P2]`
- Surfaced by: Stream 3 (P2 — explicit Linear + Azure DevOps missing), partly Stream 1 (gateway → adapters/github VerifyWebhookSignature works for GitHub)
- Files: `internal/adapters/azuredevops/webhook.go`, `internal/adapters/linear/webhook.go`
- Issue: 6/8 webhook handlers verify HMAC; these two don't. No signature tests either.
- Fix: Add `VerifySignature` per platform docs (Linear sends `linear-signature` header; Azure DevOps supports basic-auth header); mirror `plane/webhook_test.go` for test coverage.

### CS-9. Rate-limit handling silently bypassed on most GitHub API paths — `[P1]`
- Surfaced by: Stream 2 (P1 retry helpers dead code), Stream 5 (no metric for rate-limit hits), Stream 8 (smart retry default off)
- Files: `internal/adapters/github/client.go:119-164` (`doRequest` has no retry wrapper); `internal/adapters/github/retry.go` defines `WithRetry`/`WithRetryVoid` but only 20 of 51 client methods wrap calls with it.
- Issue: Endpoints like `GetPullRequest`, `GetCombinedStatus`, `ListIssues`, `ListReleases`, `GetTagForSHA`, `GetJobLogs` surface transient 429/5xx as terminal errors.
- Fix: Move retry into `doRequest` itself (wrap the `httpClient.Do` + status-check loop with `WithRetry`); plus thread `metricsRecorder.RecordAPIError` (currently dead code per Stream 5 P1) into the call site.

### CS-10. Documentation completely out of sync with code reality — `[P1]` (composite)
- Surfaced by: Stream 7 (multiple P1s); confirmed by Stream 1 (FEATURE-MATRIX missing recent work); Stream 6 (no incident-class taxonomy)
- Sites: CLAUDE.md says `v2.53.0 | 316 features` (real: v2.149.3); FEATURE-MATRIX.md frozen at v2.53.0; ARCHITECTURE.md frozen at v2.56.0; CLI flag `--autopilot=` documented as primary in 4 places (it's a hidden alias); `pitfall_git_reset_hard_in_automated_sync.md` cites live call sites for a shipped fix; `docs-and-history.md` says "Nextra v2.13.0 + Next.js 14" (real: nextra ^4.0.0 + next ^15.0.0).
- Impact: Every auto-loaded Claude Code session starts with bad ground truth. Operators searching pitfalls find phantom warnings. Doc-driven decisions are made on stale data.
- Fix: Replace prose version/feature counts with `{CURRENT_VERSION}` MDX interpolation; auto-sync via `docs-version-sync.yml` (already exists for `version.ts`); add `resolved:` frontmatter convention for memories + auto-move to `pitfalls/resolved/`; delete Project Structure tree from CLAUDE.md (replace with link); rewrite CLI Flags from `pilot start --help`.

---

## 2. Top 10 recommended actions

Ranked by leverage (impact × ease). Each is small enough to land in one PR.

| # | Action | Effort | Leverage | Refs |
|---|---|---|---|---|
| 1 | Add `IsTaskShipped(row)` predicate + reroute notifier and poller through it + cross-site invariant test | M | **Closes TASK-288 root cause; eliminates #1 recurring bug class** | CS-1 |
| 2 | Add `safeGo()` panic-recovery wrapper in `internal/logging`, sweep all 15+ production `go func()` sites | S | **Removes whole-daemon-crash risk class** | CS-3 |
| 3 | Add `pilot_poller_skipped_total{repo, reason}` + `_dispatched_total{repo}` counters at each skip branch in poller.go | S | **Makes 2026-05-21-class dispatch incidents detectable in <1 scrape interval** | CS-4 |
| 4 | Move `WithRetry` wrapper into `doRequest` of github client + wire `RecordAPIError` | S | **Restores rate-limit handling on 31 endpoints currently silent** | CS-9 |
| 5 | Flip `quality.parallel` default to `false` (or auto-detect JS-only); document race in SOP | XS | **Eliminates the 11-spurious-failures incident class until per-gate `GOCACHE` isolation ships** | Stream 8 P1; Stream 3 P1 |
| 6 | Sweep CLAUDE.md + FEATURE-MATRIX.md + ARCHITECTURE.md + `--autopilot=` doc refs; introduce `{CURRENT_VERSION}` interpolation; mark stale Navigator pitfalls `resolved:` | M | **Restores trust in auto-loaded context for every session** | CS-10 |
| 7 | Add HMAC signature verification to Linear + Azure DevOps webhook handlers | S | **Closes 2 unauthenticated webhook ingress paths** | CS-8 |
| 8 | `config.Save()` writes 0644 — change to 0600 + chmod parent 0700 | XS | **Tokens currently world-readable post-save** | Stream 8 P1 |
| 9 | `getMainBranchSHA` hardcodes `"main"` — read `ResolvedEnv().Branch` instead | XS | **Silent post-merge-CI bypass on non-main repos** | Stream 8 P1 |
| 10 | Consolidate 7 per-adapter `*_processed` tables into existing generic `adapter_processed` table; delete ~600 LOC of duplicated state_store methods | M | **Removes the "new adapter = 8th table" anti-pattern** | CS-6 |

**Sequencing hint:** 5 → 8 → 9 (XS, deploy immediately), then 2 → 3 → 4 → 7 (S, one sprint), then 1 → 6 → 10 (M, requires plan).

---

## 3. Findings by stream

### 3.1 Architecture & boundaries

**Stream summary:** Generally well-structured (interface-at-consumer used correctly in poller.go). Three systemic issues: (1) `github` adapter imports `executor` creating mirror-of-mirror coupling held open by `pilot.go` sitting above both; (2) `autopilot` imports concrete `github`/`telegram` instead of interfaces; (3) `cmd/pilot/` contains substantial business logic that should live in `internal/`.

**Strengths:** Functional options pattern consistently applied; SQL migrations idempotent; interface-at-consumer pattern correctly used in `poller.go:34-80`.

**Findings:**

- **[P1] `autopilot` directly imports concrete adapter packages (`github`, `telegram`)** — `internal/autopilot/feedback_loop.go:9`, `telegram_notifier.go:8`, `scope_guard.go:7`, `releaser.go:11`, `auto_merger.go:10`, `ci_monitor.go:12`, `controller.go:15`, `deployer.go:15`, `release_summary.go:13`. The `Notifier` interface at `controller.go:65` was the right direction — finish the job for `GitHubOperator`.
- **[P1] `autopilot_processed` table has no repo namespace — cross-repo issue collision** — `internal/autopilot/state_store.go:65-69`. PRIMARY KEY is `(issue_number INTEGER)` only. Issue #21 in repo A blocks dispatch of issue #21 in repo B. Documented in TASK-288. Fix: `ALTER TABLE` add `repo TEXT NOT NULL DEFAULT ''`, change PK to `(repo, issue_number)`.
- **[P2] `internal/adapters/github/poller.go` imports `internal/executor` (adapter → executor coupling)** — lines 14, 117, 212, 520-535, 872. Near-cycle risk; held open only by `pilot.go`/`orchestrator.go` sitting above both. Fix: extract `RateLimitError`, `ParseRateLimitError`, `ExtractDirectoriesFromText` to `internal/ratelimit` or `internal/text`.
- **[P2] `internal/config/config.go` imports all 10 adapter packages plus autopilot/executor/quality** — lines 13-33. Widest fan-in node in the dependency graph. Fix: replace embedded `*Config` structs with `yaml.RawMessage` or move config structs to standalone `internal/adapters/*/config` sub-packages.
- **[P2] `internal/autopilot/state_store.go` owns adapter-specific tables for 6 non-GitHub adapters** — lines 80-124 (`linear_processed`, `gitlab_processed`, `jira_processed`, `asana_processed`, `azuredevops_processed`, `plane_processed`). Generic `adapter_processed` (`state_store.go:118`) added as future fix but legacy tables still created. Fix: move into `internal/memory/store.go`; route through existing `ProcessedStore` interface (`registry.go:59`).
- **[P2] `internal/executor/runner.go` is a 4,556-line god file with 111 methods** — `Runner` struct has 40+ injectable fields covering prompting, git, worktrees, epics, quality, progress, metrics, learning, token limits, intent judging, retry, self-review, knowledge graph, Navigator phase tracking. Fix: extract `PromptBuilder` (started), `ProgressTracker`, `RetryOrchestrator`, `MetricsFlusher` and delegate. Target ≤15 fields.
- **[P2] Duplicate `IssueAllowlist` interface in both `executor` and `adapters/github`** — `internal/executor/repo_guardrail.go:50` and `internal/adapters/github/issue_create.go:27`. Comment admits the workaround is for cycle avoidance. Fix: extract shared interface to zero-dep `internal/allowlist` package.
- **[P2] `HasCompletedExecution` lacks deliverable guard** — `internal/memory/store.go:540-551`, `internal/adapters/github/poller.go:1022-1038`. See CS-1.
- **[P3] `cmd/pilot/main.go` is 2,988 lines** with `runPollingMode` ~1,400 lines and three interface shim types (`storeTaskChecker`, `preFlightJudgeShim`, `storeExecutionSaver`) at lines 2800-2848. Untestable. Fix: introduce `internal/startup/` package.
- **[P3] `TelegramNotifier` is the only concrete notifier and lives in `autopilot`** — `controller.go:65` defines `Notifier` interface (correct location); `telegram_notifier.go:8` keeps the concrete impl coupled. Fix: move `TelegramNotifier` to `internal/adapters/telegram/` or `cmd/pilot/`.
- **[P3] `internal/executor/epic.go` imports `"testing"` in a production source file** — lines 16, 621 use `testing.Testing()` as runtime gate. Smells; ships `testing` package into every production binary. Fix: replace with injectable function field on `Runner` or `TaskDecomposer`.
- **[P3] `internal/orchestrator/orchestrator.go` imports 6 adapter packages directly** — lines 11-18. Should consume `adapters.Adapter` from registry.
- **[P3] `internal/autopilot/controller.go` uses `*memory.Store` concrete in two setter entry points** — lines 117-120 and 256-259. Internally stored as `approvalPersister` interface. Fix: leak the interface instead.
- **[P3] `internal/pilot/pilot.go` is 1,500-line god struct, 43 methods, 35 fields** — service-locator anti-pattern. Fix: extract per-adapter `Manager` types.
- **[P3] `internal/alerts/channels.go` imports concrete `slack` and `telegram`** — lines 15-16. Reverses dependency direction.
- **[P4] `NewStateStoreFromPath` opens its own SQLite connection** — `state_store.go:29-40`, duplicates pragma logic from `memory/store.go:39-49`. Fix: remove; tests use `memory.NewTestDB`.
- **[P4] Three nearly-identical adapter cleanup files** — `github/cleanup.go`, `gitlab/cleanup.go`, `azuredevops/cleanup.go`. Fix: centralize in `memory.CleanupAdapter`.
- **[P4] `internal/wiring/harness.go` imports `e2e/mocks`** — line 11. Crosses `internal/` boundary. Fix: move to `internal/testutil/mocks/`.

---

### 3.2 Code quality & tech debt

**Stream summary:** ~30k LOC across 28 packages with complexity concentrated in 4 files. Massive cross-adapter duplication (3 pollers × 90% identical, 6 messenger implementations, 7 `*_processed` tables). Retry helper exists but is unused on 31 of 51 GitHub client methods. Zero SQL transactions anywhere. Single 2,203-line god function in executor.

**Hotspot files (top 10 by LOC):**
1. `internal/executor/runner.go` (4,556)
2. `internal/dashboard/tui.go` (2,842)
3. `internal/autopilot/controller.go` (2,532)
4. `internal/memory/store.go` (2,017)
5. `internal/adapters/github/poller.go` (1,727)
6. `internal/executor/epic.go` (1,582)
7. `internal/pilot/pilot.go` (1,500)
8. `internal/adapters/telegram/handler.go` (1,211)
9. `internal/adapters/github/client.go` (1,091)
10. `internal/executor/worktree.go` (1,042)

**P1 findings:**

- **[P1] Retry helpers are dead code on most GitHub API paths** — `internal/adapters/github/client.go:119-164`, `internal/adapters/github/retry.go`. See CS-9.
- **[P1] No SQL transactions anywhere — multi-write operations are not atomic** — `internal/autopilot/state_store.go`, `internal/memory/store.go`. `rg 'Begin\(|BeginTx'` returns zero hits in production. Migrations, multi-row inserts, delete+insert sequences all run as separate `db.Exec`. Fix: `withTx(func(*sql.Tx) error) error` helper.
- **[P1] `time.Sleep` inside poller loops blocks context cancellation** — `internal/adapters/github/poller.go:479,614,633,644`; same pattern in gitlab and azuredevops pollers. After errors the poller calls `time.Sleep(5 * time.Minute)` instead of `select { case <-ctx.Done(): ...}`. SIGTERM may wait up to 5 minutes per stuck poller.
- **[P1] OnPRCreated dispatch path duplicated and divergent** — `internal/adapters/github/poller.go:564-592` (sequential) and `1151-1190` (parallel). The "diagnostic + notify autopilot" block is duplicated verbatim including 4 copies of the TASK-60 marker comment. Future fixes need to be applied twice — exact surface where v2.149 dispatch incidents originated.
- **[P1] `WithOnIssue` legacy callback only used by tests but kept in production API** — `internal/adapters/github/poller.go:172-178` and parallel branch at 1180. Zero production callers. If a future caller wires the wrong option, the autopilot never sees the PR. Fix: delete `WithOnIssue` + `onIssue` field + dead branch.
- **[P1] Goroutine leak risk on context cancel between `wgMu` unlock and `go func`** — `internal/adapters/github/poller.go:1125-1127`. If downstream blocks on non-context-aware operation, Drain hangs.
- **[P1] Issue grace-period unmark is non-atomic between in-memory map and store** — `internal/adapters/github/poller.go:775-785` and `997-1006`. If DB unmark fails, in-memory state diverges from persistent state.
- **[P1] Sequential mode does not refresh labels before dispatch (parallel mode does)** — `internal/adapters/github/poller.go:691-818` (sequential) vs `1069-1084` (parallel does fresh `GetIssue`, GH-2341). Sequential can dispatch an issue that was already marked `pilot-done` between list and dispatch.

**P2 highlights:**

- **[P2] `runner.executeWithOptions` is 2,203 lines** — `internal/executor/runner.go:1133`.
- **[P2] `checkForNewIssues` (287 lines) and `findOldestUnprocessedIssue` (128 lines) share 80% of filter logic** — `internal/adapters/github/poller.go:906-1192` and `691-818`.
- **[P2] 3 issue pollers with ~90% identical structure** — github/gitlab/azuredevops.
- **[P2] Per-adapter `*_processed` SQLite tables (7 copies)** — `internal/autopilot/state_store.go:65-114, 276-600`. ~600 LOC duplicated boilerplate.
- **[P2] 6 `Send*` messengers with identical interface, divergent error handling** — telegram/slack/discord. 5-6 positional `string` parameters invite caller errors.
- **[P2] Cleanup files duplicated across github/gitlab/azuredevops**.
- **[P2] No `context.Context` discipline in `controller.go` background goroutine** — `internal/autopilot/controller.go:1700-1802`. `context.Background()` discards parent ctx; `DeleteBranch` at 1802 cannot be cancelled.
- **[P2] 417 `fmt.Errorf` calls but inconsistent `%w` wrapping** — `state_store.go` returns raw `err` 19 times; `controller.go` lines 518, 2528.
- **[P2] Banner uses bare `fmt.Println` in startup path** — `internal/banner/banner.go:39-191`. ~30 calls hardcoded to stdout, no way to silence.
- **[P2] No tests for `banner`, `transcription`, `testutil` packages** (testutil exempt).
- **[P2] `controller.go` has 5+ functions of 100-145 lines** — handlers mix label management, GitHub API calls, state transitions, metrics, notifications. Fix: `StageHandler` interface.
- **[P2] `state_store.migrate` is a 105-line linear list of SQL strings** — no versioning, no rollback.

**P3 selected (full list in agent output):**

- 5-7 positional string parameters in messengers and worktree functions.
- `Get*` vs `List*` vs `Search*` vs `Find*` naming inconsistency in GitHub client.
- `MergeMethod string` instead of typed enum.
- `RequiredChecks []string` deprecated for 2+ versions but still loaded with no warning.
- Magic timeouts scattered (`30 * time.Minute` literal repeated 4× in controller.go).
- `5 * time.Minute` shared between unrelated retry-grace and poller-sleep paths.
- `os.Getenv` scattered in 6+ places without central registry.
- 2 unused `TODO(GH-…)` markers (`metrics.go:39` GH-2836 means autopilot metrics reset every restart; `teams/service.go:372` GH-784).
- Diagnostic log block left in production hot path (poller.go GH-2999 closed).
- Triple-duplicated 70-char comment block in poller.go.
- `IsTaskQueued` checks string-formatted task ID `"GH-%d"` — ID encoding leaks across packages.
- `RetryStateLabels` exposed as package-level mutable slice.

**P4 selected:**

- `time.Sleep(time.Nanosecond)` in `webhooks/event.go:114`.
- Deprecated fields with no removal target in `dispatcher.go:20,49` and `runner.go:1052`.
- Comment "~10% of failures are 'No commits between...'" — hard-coded percentage in comment.
- `var ErrConflict = fmt.Errorf(...)` should use `errors.New` (telegram/slack).
- `(*Poller).Reset()` re-allocates empty map; should `clear()` (Go 1.21+).
- `_, _ = w.Write(indexFile)` discards both return values in `gateway/frontend.go:93`.

---

### 3.3 Test coverage & quality

**Stream summary:** Aggregate ratios look healthy (70-260%) but distribution hides 3 untested packages and `internal/pilot` at 7.8% containing 1,500 LOC of main glue. The v2.149.3 hotfix has a regression test (`TestAllChildrenDone`); the 11-failure quality-runner race has NO regression test. Zero `t.Parallel()` usage anywhere; zero benchmarks; zero fuzz tests; no `TestMain`. 137 `time.Sleep`-based test syncs across the codebase. Race detector is enabled in `make test` and CI (good).

**Coverage map summary:**
- **Packages with 0 tests:** `internal/banner` (267), `internal/transcription` (340), `internal/testutil` (93, exempt).
- **Packages <30%:** `internal/pilot` (7.8%).
- **Critical files without sibling tests:** `executor/backend_anthropic.go` (784 LOC), `executor/parallel.go` (374), `executor/runner_decompose.go` (232), `executor/markers.go` (227), `executor/diagnose.go` (175), `executor/lint.go` (140), `autopilot/metrics_persister.go` (111), `pilot/pilot.go` (1,500).
- **Critical paths with thin sibling tests:** `executor/backend.go` (905→246, 27%), `executor/progress.go` (565→180, 31%), `executor/gate.go` (232→96, 41%).

**Findings:**

- **[P1] Anthropic API backend has zero sibling tests** — `internal/executor/backend_anthropic.go:1`, 784 LOC. Compare to 1,073 LOC `backend_claudecode_test.go`. Fix: `httptest.Server` mocking SSE streams.
- **[P1] No regression test for parallel quality-gate cache-contention race** — `internal/quality/runner.go:74-89` and `runner_test.go:759-786`. The recent 11-failure incident has no regression test; existing test only asserts wall-clock <250ms. Fix: add `TestRunner_RunAll_ParallelGOCACHEIsolated`.
- **[P1] `internal/pilot/pilot.go` (1,500 LOC) at 7.8% ratio** — Only `webhook_handlers_test.go` exists. Component-startup bugs surface only in production.
- **[P1] `transcription` package (Whisper API) has zero tests** — `internal/transcription/transcriber.go`, `whisper_api.go`. Voice-message regressions ship invisibly.
- **[P2] Discord adapter skeletally tested** — only `handler_test.go` exists. `client.go`, `transport.go`, `notifier.go`, `formatter.go`, `types.go` have no direct coverage.
- **[P2] Plane adapter `poller.go` (17k LOC) has no `poller_test.go`** — `internal/adapters/plane/poller.go:1`. Every other ticket-system adapter has a `poller_test.go`.
- **[P2] 137 `time.Sleep`-based test syncs across codebase** — `internal/alerts/engine_test.go` alone has ~40 occurrences. Fix: `require.Eventually` or `clock.Mock`.
- **[P2] Tests with multi-second `time.Sleep` (slowest)** — `internal/upgrade/upgrade_critical_test.go:77, 96` (2s each); `internal/adapters/github/poller_integration_test.go:281, 448`; `telegram/handler_test.go:801` (`startupAPITimeout + 1s`).
- **[P2] Webhook signature verification missing for Azure DevOps and Linear** — `internal/adapters/azuredevops/webhook.go`, `internal/adapters/linear/webhook.go`. See CS-8.
- **[P2] No `t.Parallel()` anywhere** — zero usage across all `_test.go`.
- **[P2] Hardcoded `/tmp/...` paths in tests instead of `t.TempDir()`** — `internal/upgrade/upgrade_test.go:1075`; `restart_test.go:177, 211`; `memory/store_test.go:1757, 1788`; `quality/executor_test.go:12`; `memory/pr_state_test.go:18`; `config/config_test.go:1405-1428`; `upgrade/upgrade_critical_test.go:380`. Plus tests that actually `os.WriteFile` to real `/tmp` (`upgrade/restart_test.go:25, 211`; `upgrade/upgrade_test.go:281, 339, 372, 406, 582, 642, 669, 724`).
- **[P3] Ad-hoc test tokens used instead of `internal/testutil` constants** — `internal/health/health_test.go` (8 sites); `internal/adapters/gitlab/poller_test.go` (10 sites); `gitlab/merger_test.go`; `linear/types_test.go`, `multi_workspace_test.go`; `jira/adapter_test.go`, `notifier_test.go`; `github/poller_preflight_test.go` (5 sites). ~30 sites total. Fix: mechanical refactor + `make check-test-tokens` lint.
- **[P3] `executor/backend.go` 27% test ratio** — effort-routing and model-selection logic may have subtle bugs.
- **[P3] `executor/progress.go` 31% test ratio** — concurrent writes, customer-visible UI bug class.
- **[P3] Real-network tests use `http.Get` against `httptest.Server` URLs instead of calling package methods** — `slack/client_test.go:246, 425, 851`; `upgrade/upgrade_test.go:1313`. Tests pass even if SUT is gutted.
- **[P3] Many tests rely on host `git`/`cloudflared`/`ngrok` and silently `t.Skip` when absent** — `executor/git_test.go` (6 skips); `executor/runner_git_test.go`; `executor/repo_guardrail_test.go:202`; `executor/preflight_test.go:42`; `tunnel/*_test.go` (12+ skips). Convert to `t.Fatal` when CI=true.
- **[P3] `t.Helper()` missing in autopilot helper files** — `deployer_test.go`, `ci_monitor_test.go`, `scope_guard_test.go`, `metrics_test.go`, `auto_merger_test.go`, `types_test.go`, `release_summary_test.go` (0 calls each).
- **[P3] No benchmarks (`Benchmark*`), no fuzz tests (`Fuzz*`)** — zero across codebase. Fuzz `internal/webhooks`, `executor/prompt_builder.go`, `intent`, signature verifiers.
- **[P3] No `TestMain` anywhere** — no shared setup/teardown for DB-backed tests, no `goleak.VerifyTestMain`.
- **[P3] `TestRunner_RunAll_ParallelExecution` uses real `sleep 0.1` shell commands** — `runner_test.go:763-786`. Wall-clock dependent (CI flake) + spawns shell.
- **[P4] No test for v2.149.3 IsEpic suppression path** — `cmd/pilot/handlers.go`, `commands.go`. `TestAllChildrenDone` exists (good); the "no-changes-applied + pilot-failed label" suppression for `IsEpic` does not have a direct test.
- **[P4] Tests skipped with vague rationale** — `config_test.go:448`, `memory/query_test.go:221`.
- **[P4] Integration tests are tag-gated** — verify `.github/workflows/ci.yml` actually invokes `make test-integration` and not just `make test`.

---

### 3.4 Observability & error handling

**Stream summary:** Logging infra is solid (~71/95 files use structured logger; stdlib `log` only in config.go). Largest gap is counter coverage on poller skip-reasons and dispatch decisions — exact class of blind spot that caused 2026-05-21 silent dispatch incident. OOM kill detection is half-wired (event classified, no AlertType, no counter). Production goroutines have zero panic recovery. `Metrics.RecordAPIError` is dead code in prod. No correlation/trace IDs propagated.

**Logging posture:**
- ~71/95 production files use `logging.With…` or `*slog.Logger`.
- Direct `slog.Default()` in 10 production files: `internal/alerts/dispatcher.go:45`, `engine.go:101`, `autopilot/controller.go:195`, `feedback_loop.go:33`, `metrics_alerter.go:104`, `deployer.go:38`, `executor/intent_judge.go:48`, `signal.go:43`, `tunnel/manager.go:65`, `webhooks/manager.go:48`.
- Stdlib `log`: only `internal/config/config.go`.
- Most-violating files for bare `return err`: `memory/store.go` (28), `autopilot/state_store.go` (21), `teams/store.go` (9), `adapters/telegram/notifier.go` (9), `memory/knowledge.go` (7), `executor/worktree.go` (7), `autopilot/telegram_notifier.go` (6), `tunnel/cloudflare.go` (5).

**Metric coverage gaps (critical):**
- Poller skip-reason counter — none of 9 branches counted.
- OOM kills — classified, alerted, but no `pilot_oom_killed_total` counter.
- Subprocess exit code distribution — classified but not exported.
- Peak RSS — in SQLite, not in Prometheus.
- API call counts/latencies — `RecordAPIError` never called.
- Rate-limit hits, approval wait time, merge wait, repo guardrail trips, stagnation events, smart-retry triggers, pre-flight judge decisions, worktree create failures, backend-type breakdown — all uninstrumented.

**Findings:**

- **[P1] No counter for poller skip-by-reason — same blind spot that caused 2026-05-21 silent dispatch loss** — `internal/adapters/github/poller.go:925-1040`. See CS-4.
- **[P1] No counter or alert for repo allowlist bypass** — `internal/executor/repo_guardrail.go:81-94`. `PILOT_ALLOW_UNMANAGED_REPO=1` only logs `slog.Warn`. TASK-286 incident (Pilot wrote 6 stray issues to upstream `ylcn91/pilot`) is exact class that should trigger P1 alert.
- **[P1] OOM kill threshold alerting is not wired** — `internal/alerts/types.go` has no `AlertTypeOOMKilled`; `engine.go:183` only routes to generic `handleTaskFailed`. No `pilot_oom_killed_total` counter. v2.148 hardening has no companion alert.
- **[P1] `Metrics.RecordAPIError` is unused — `pilot_api_errors_total` always zero** — `internal/autopilot/metrics.go:118`, called only from tests. GitHub client error paths never call it. `AlertTypeAPIErrorRateHigh` (`alerts/engine.go:672`) will never fire.
- **[P1] Production goroutines have no panic recovery** — See CS-3 for full file list.
- **[P2] No correlation/trace ID propagated through pipeline** — `internal/logging/logging.go:155-196` defined, never used. Cross-component incident reconstruction requires manual grep+join.
- **[P2] No `pilot_oom_killed_total` / `pilot_backend_exit_code_total` Prometheus counters** — `executor/runner.go:1963`, `gateway/prometheus.go`. Capacity-planning queries need ad-hoc SQL.
- **[P2] No dispatch-decision metric (deferred-by-scope, scope-grouped)** — `poller.go:1054-1060`. Cannot tune `max_concurrent`.
- **[P2] Approval wait time + merge-wait duration not measured** — `autopilot/controller.go`. Cannot answer "is approval the bottleneck?".
- **[P2] `HasCompletedExecution` errors silently ignored in dispatcher stale-recovery** — `executor/dispatcher.go:208,249`. `completed, _ := d.store.HasCompletedExecution(...)` — error return discarded. Reproduces TASK-288 dispatch bug pattern.
- **[P2] Quality-gate parallel failure does not emit structured "spurious" signal** — `quality/runner.go:75-90`. 2026-05-21 11-failure cluster looked like normal isolated failures.
- **[P2] Webhook delivery results not exposed as metric** — `webhooks/manager.go:60-89`. Silent webhook failures accumulate.
- **[P2] Alerts engine drops events when queue full with no metric** — `alerts/engine.go:150-157`. Sustained drops only visible via log scraping.
- **[P2] Dispatcher worker crash → silent task loss** — `executor/dispatcher.go:437`. Worker goroutine has no panic recovery; no counter for worker death.
- **[P3] `slog.Default()` used in 10 production files instead of `logging.WithComponent`** — see logging posture above.
- **[P3] Sentinel errors are scarce; many checks use `strings.Contains`** — `executor/ratelimit.go:18-23`, `memory/store.go:341, 372-374`. Brittle to upstream message changes.
- **[P3] Many bare `return err` in store paths lose call-site context** — `memory/store.go`, `autopilot/state_store.go`.
- **[P3] `OnPRCreated skipped: …` diagnostic logged twice (sequential + parallel) with duplicated comments** — `poller.go:564-581` and `1151-1168` (4× repeated 70-char comment block).
- **[P3] Pre-flight judge runs twice in sequential mode and again in parallel — no metric distinguishing path** — `poller.go:500-515` and `1086-1101`.
- **[P3] Telegram/Slack/Discord notifier failures use `slog.Warn` with no metric** — silent "alerts pipeline is broken" mode.
- **[P3] `/health` always returns 200 regardless of state** — `gateway/server.go:344-350`. K8s liveness probe against `/health` won't restart wedged binary.
- **[P3] Bypass-allowlist log uses `slog.Warn` global (not component logger)** — `executor/repo_guardrail.go:86-92`.
- **[P3] Dispatcher "Failed to fetch issues" Warn has no repo label** — `poller.go:913`. Multi-repo daemons can't sort failures by repo.
- **[P3] No metric for `markProcessed` writes per tick** — `poller.go:1194-1206`.
- **[P3] Goroutine leaks possible — release-summary goroutine has no concurrency cap** — `controller.go:1700`.
- **[P4] `RecordPRConflicting` and `RecordLabelCleanup` defined but never called in prod** — `autopilot/metrics.go:104,130`. `pilot_prs_conflicting_total` and `pilot_label_cleanups_total` always zero.
- **[P4] `pilot_active_prs_total` derived field computed from `ActivePRsByStage` sum** — `prometheus.go:146`. Subtle bug if new stage added.
- **[P4] `escapeLabel` uses O(n) string concat** — `prometheus.go:280-294`. Fix: `strings.Builder`.
- **[P4] No log line on alert engine startup includes effective rule count by type** — `alerts/engine.go:125-128`. Operator can't tell if specific rules wired.

---

### 3.5 Regression pattern analysis

**Stream summary:** 4 patch releases in ~24h pre-workshop (v2.149.0 → v2.149.3); emergency band-aids dominate, root-cause work deferred. Same bug class patched repeatedly across versions. Two large systemic gaps drive most fixes: (1) no single source of truth for "task completed"; (2) silent skip paths everywhere. The OAuth provider cascade (Apr 2026, 10 reverts in `891affe3..424b0198`) shows Pilot has previously generated and merged ~10 PRs of plausible-but-unwanted code from hallucinated subtasks under a closed parent — same root cause as GH-201.

**Recurring bug classes** (see CS-1 through CS-5 for the top 5):

1. **"Completed" semantic disagreement across stores** — CS-1, P1.
2. **Closed-parent / stale-state dispatch loop** — CS-2, P1.
3. **Silent skip / silent disable** — CS-4, P2.
4. **Order-of-operations in multi-gate scanners** — covered below, P2.
5. **Automated git operations that lose user/Pilot work** — CS-5, P1.
6. **Subprocess robustness** — covered below, P2.
7. **Allowlist / namespace correctness** — covered below, P2.
8. **Approval / TG-callback wiring** — closed at v2.129.0, included for completeness, P3.

**Additional findings:**

- **[P1] Rapid-release cadence (4 patches in 24h pre-demo) indicates pre-release test confidence is low** — `b31275cb..fd0f7b69` in 21h. Tests reproduce reported bugs, not characterize systems. Fix: add pre-release "soak" stage running Pilot against staging repo with synthetic issues representing prior 5 incident classes.
- **[P2] Multi-gate scanner ordering invented per-feature, no phase discipline** — `internal/autopilot/controller.go`. `b140e441` had to hoist recorder before three skip gates that `c54dc79b` placed it after. `6df99408` added instrumentation because `OnPRCreated` was never firing in stage mode. Fix: refactor `ScanRecentlyMergedPRs` and `checkForNewIssues` into explicit `observe() → classify() → dedup() → act()` phases; assert observation always runs.
- **[P2] Subprocess migration is half-finished — telemetry/retry/rlimit added but not validated end-to-end** — `executor/rss_sampler*.go`, `resource_limits_*.go`, `retry.go`. RLIMIT_AS path default-off; darwin telemetry-only; no integration test deliberately OOMs subprocess. Fix: CI job using synthetic memory-hog to validate retry path; document Claude-Code subprocess-tree behavior in SOP.
- **[P2] Allowlist correctness depends on path-string semantics that vary by OS/setup** — `cmd/pilot/repo_allowlist.go`. Phase C uses `git rev-parse --git-common-dir` but no `realpath`/symlink normalization, no case-folding for macOS HFS+, no submodule handling. Fix: resolve both sides through `filepath.EvalSymlinks` + canonical case-folding.
- **[P3] Tests reproduce the reported scenario, not the bug class** — `executor/epic_test.go` (12 fix-commit touches), `autopilot/controller_test.go` (16). `d659d73e` added 2 GH-201 cases; `59c1d252` had to add 2 more for non-terminal labels. Pattern-level invariants not asserted. Fix: architecture-level "predicate consistency" tests.
- **[P3] `worktree.go` reset-hard comment claims safety with same reasoning that was wrong for `syncMainBranch`** — `internal/executor/worktree.go:349`. Comment is not a guard. Fix: precondition check.
- **[P3] No persisted lineage between "v2.X.Y patch" and the incident class it fixes; release notes are commit titles** — All `docs: sync version` commits since `6a787501` (v2.135.0). Users can't decide which v2.149.x to pin. Fix: `.agent/system/incident-classes.md` mapping fix-commits to bug classes.
- **[P4] Orphan documentation: `19b50cba` and `3db99e9d` are identical commits documenting same `OnPRCreated` gate** — rebase/cherry-pick hygiene issue.

Fix-flux table in Appendix B.

---

### 3.6 Documentation drift

**Stream summary:** CLAUDE.md dramatically out-of-date (claims v2.53.0/316 features, real is v2.149.3); FEATURE-MATRIX frozen at v2.53.0; ARCHITECTURE.md frozen at v2.56.0 (3 months/93 versions behind); `--autopilot=` flag documented as primary in 4 places (it's a hidden alias); at least one pitfall memory dangerously cites live call sites for shipped fixes.

**Version drift (selected; full table in Appendix):**

| File | Claimed | Actual | Drift |
|---|---|---|---|
| `CLAUDE.md:239` | v2.53.0 / 316 features | v2.149.3 | 96 minors behind |
| `.agent/system/FEATURE-MATRIX.md:3` | "(v2.53.0)" | v2.149.3 | label fossilized |
| `.agent/system/ARCHITECTURE.md:3` | v2.56.0 (2026-03-04) | v2.149.3 (2026-05-25) | ~93 minors / 3 months |
| `.agent/system/docs-and-history.md:6` | Nextra v2.13.0 + Next 14 | Nextra ^4.0.0 + Next ^15.0.0 | major version drift |
| `docs/lib/version.ts:4` | v2.149.2 | v2.149.3 | 1 patch (auto-sync didn't fire for v2.149.3) |
| `docs/content/getting-started/installation.mdx:48` | v2.102.2 | v2.149.3 | inline example stale |
| `docs/content/cli/commands.mdx:589,2601,2611-12` | v2.102.2 / `--autopilot=` | v2.149.x / `--env=` | all inline strings stale |

**Findings:**

- **[P1] FEATURE-MATRIX.md frozen at v2.53.0 — 90+ versions of features undocumented** — `.agent/system/FEATURE-MATRIX.md:3,461`.
- **[P1] CLAUDE.md "Current Status" block is 96 versions stale** — `CLAUDE.md:239`. Auto-loaded into every session.
- **[P1] `pitfall_git_reset_hard_in_automated_sync.md` claims live call sites that have been fixed** — frontmatter `files:` cites 3 sites that all shipped fixes (TASK-283 / v2.146.7). `nav-graph "git reset"` returns active critical pitfall for already-fixed code.
- **[P1] CLI flag drift: `--autopilot=` documented as primary in 4 places; actually hidden alias** — `cmd/pilot/main.go:1173-1178` (truth) vs CLAUDE.md, `.agent/DEVELOPMENT-README.md:274`, `.agent/system/PR-CHECKLIST.md:3`, `docs/content/cli/commands.mdx:22,41,50`.
- **[P1] Nextra version in docs-and-history.md two majors out of date** — `.agent/system/docs-and-history.md:6,28`. Claims "Nextra v2.13.0 + Next.js 14", real is ^4.0.0 + ^15.0.0. Pinning gotcha is obsolete.
- **[P2] CLAUDE.md "Project Structure" lists 9 of 28 `internal/` packages** — `CLAUDE.md:130-147`. Missing: approval, banner, briefs, budget, comms, health, intent, logging, orchestrator, pilot, quality, replay, teams, text, transcription, tunnel, upgrade, webhooks, wiring.
- **[P2] CLAUDE.md says Nextra v2 on line 145 and Nextra v4 on line 256** — internal contradiction.
- **[P2] DEVELOPMENT-README CLI Flags section mixes v0.x and v2.x** — lines 271-280.
- **[P2] ARCHITECTURE.md frozen at v2.56.0** — lines 3, 712-717. Recent Changes table ends at v2.56.0. Package audit dated 2026-03-04 lists 34 packages (now 28).
- **[P2] FEATURE-MATRIX mis-tags subprocess OOM hardening as v2.147 when shipped v2.148** — lines 40-42.
- **[P2] CLAUDE.md "Required env vars" overstates LINEAR_API_KEY/SLACK_BOT_TOKEN** — lines 196-198. Most installs need neither.
- **[P2] docs/content/getting-started/installation.mdx hard-coded example version 47 minors stale** — line 48.
- **[P2] docs/content/cli/commands.mdx hard-coded `Pilot v2.102.2` and `--autopilot=`** — multiple lines.
- **[P2] docs/content/features/dashboard.mdx upgrade example shows v2.102.2** — lines 210, 219.
- **[P2] `feedback_subprocess_not_api.md` cites non-existent file as "violator pending fix"** — line 24 references `internal/executor/subtask_parser.go:15-40`; file does not exist.
- **[P2] DEVELOPMENT-README.md `~/.local/bin` PATH "Known Issue (GH-204)" not re-verified** — line 261. Very old issue; need to check install.sh.
- **[P3] `.agent/audits/` directory exists but is empty (until now)** — implies workflow never populated.
- **[P3] ARCHITECTURE.md key file ref `internal/autopilot/board_sync.go` does not exist** — line 374. Actual location: `internal/adapters/github/project_board.go`.
- **[P3] `pattern_decomposer_label_evaporation.md` line refs drifted** — lines 9-10 cite `epic.go:883, :1003` which are now in `recoverExistingSubIssues`.
- **[P3] `bug_handleconflict_no_refile.md` cites `controller.go:1688-1729`; function now at line 1741** — minor line drift.
- **[P3] `docs-and-history.md` "Nextra v2 deps must be pinned" gotcha is obsolete** — line 28.
- **[P3] DEVELOPMENT-README Release Workflow shows `git tag v0.X.Y` as example** — line 249.
- **[P3] CLAUDE.md "Project Overview" lists Linear/Jira/Asana as only ticket sources** — lines 100-104. Underrepresents capability.
- **[P3] `pilot upgrade` recommended in DEVELOPMENT-README without Homebrew exception note** — line 253.
- **[P3] `feedback_check_all_prompts_for_leaks.md` cites `executor/workflow.go:163`** — line numbers may have drifted.
- **[P3] `.agent/knowledge/graph.json` `updated` is `2026-05-21`, missing v2.149 commits** — line 3. `nav-graph` queries miss most recent week.
- **[P3] `docs/lib/version.ts` is one patch behind current tag** — auto-sync workflow didn't fire for v2.149.3 PR #3054.
- **[P4] CLAUDE.md "Forbidden Actions" has stale stricture about package.json** — line 224. Nextra 4 migration required edits.
- **[P4] CLAUDE.md duplicates `Pilot runs in a separate terminal` text vs DEVELOPMENT-README** — line 69.
- **[P4] `.agent/drafts/README-v2.md` and `README-v3.md` are unreviewed drafts in doc tree** — no index entry.
- **[P4] HTML-style comment in CLAUDE.md `<!-- GitHub integration verified -->`** — line 272.
- **[P4] FEATURE-MATRIX "Last Updated" date 2026-05-21 but version v2.53.0** — disagreement.

**Navigator hygiene meta-finding (P2):** Adopt `resolved:` frontmatter convention; auto-move to `pitfalls/resolved/`; eliminates false-alarm class from the 2 confirmed stale memories.

---

### 3.7 Config & operational risk

**Stream summary:** `Config.Reload` is dead code despite "hot reload" comment. Releaser frozen at startup. Post-merge CI hardcodes `"main"`. `quality.parallel` default true (cache contention). `config.Save()` writes 0644 (world-readable secrets). `os.ExpandEnv` over whole YAML — silent empty on unset vars. Smart retry, stagnation, subprocess RSS caps, autopilot, alerts, daily-brief, quality, budget — all default to disabled.

**Findings:**

- **[P1] `Releaser` instance freezes release config at construction** — `internal/autopilot/controller.go:204-205`, `releaser.go:23,327`. `NewController` evaluates `resolveRelease(cfg)` once. Switching `--env` post-startup or adding env-scoped release config silently uses old policy.
- **[P1] Post-merge CI monitor hardcodes `"main"`** — `internal/autopilot/controller.go:1588`. `getMainBranchSHA` ignores `ResolvedEnv().Branch`. Repos using `develop`/`master`/`trunk` silently fail post-merge monitoring.
- **[P1] `quality.parallel` defaults to `true` via pointer-nil** — `internal/quality/types.go:133-138`, `runner.go:62-83`. Concurrent `make build/test/lint` share `~/.cache/go-build` and `~/.cache/golangci-lint`. The 11-failure incident pattern.
- **[P1] `config.Save` writes `0644` — world-readable secrets** — `internal/config/config.go:496`. Holds GitHub PAT, Linear, Slack tokens. `cmd/pilot/backend.go:327` does 0600 for one path; canonical Save does not.
- **[P1] `Config.Reload` is dead code; "hot reload" comment is misleading** — `internal/config/config.go:509-522`. Only called from test. No SIGHUP handler in daemon main. Operators expect edit-in-place to apply; nothing happens.
- **[P1] Silent env-var expansion via `os.ExpandEnv` on whole YAML** — `internal/config/config.go:458`. Unset `${VAR}` becomes `""` silently. Also expands `$WORD` without braces (Bash-like), mangling password-like fields containing literal `$`. Misconfigured prod deployments boot with empty tokens.
- **[P2] `executor.retry.enabled` defaults to `false`** — `internal/executor/retry.go:82-83`. Rate-limit/API/timeout/OOM strategies fully designed but opt-in.
- **[P2] `executor.stagnation.enabled` defaults to `false`** — `internal/executor/backend.go:835`. Stuck-task detection off by default.
- **[P2] Model routing enabled but all tiers point to same model** — `internal/executor/backend.go:758-767`. `DefaultModelRoutingConfig` has Trivial=haiku and Simple/Medium/Complex all `claude-sonnet-4-6`. Operators believe routing saves money; only trivial branches.
- **[P2] Per-env Release config does not propagate to existing Releaser** — `internal/autopilot/controller.go:200-206`. No `Controller.SetReleaseConfig` or `Releaser.Reload`.
- **[P2] `executor.heartbeat_timeout` not validated at config load** — silently clamped to [1m, 30m]. Operators don't notice setting ignored.
- **[P2] `default_alert_rules` enabled but no channel wired by default** — `config/config.go:380-440`. Two layers of "looks configured" but inert.
- **[P2] `BackendConfig.Type` default `"claude-code"` not validated** — typo silently selects fallback.
- **[P2] `autopilot.environment` is "deprecated" but still drives `EnvironmentName()`** — `internal/autopilot/types.go:80-86, 249-259`. `DefaultEnvironment` field never read anywhere.
- **[P2] `executor.api_auth_token` `${ENV_VAR}` expansion silently empties** — depends on global env-expansion finding.
- **[P3] No `pilot env` / no env-var registry doc** — bypass and override vars only discoverable by grep.
- **[P3] SQLite `db.SetConnMaxLifetime(0)`** — fine today; footgun if `MaxOpenConns` ever bumps.
- **[P3] SQLite migrations are append-only and not versioned** — `internal/memory/store.go:71-165`. 29 ALTER statements every startup; "duplicate column" errors swallowed.
- **[P3] No SQLite backup/rotation policy** — `pilot.db` grows unbounded.
- **[P3] `GracefulUpgrader.UpgradeOptions.TaskTimeout=5m` hardcoded; no config knob** — `upgrade/graceful.go:55-61`. Long epic tasks exceed.
- **[P3] `wait-for-tasks` only checks running task IDs; no queue drain** — `upgrade/graceful.go:78-100`. Pending tasks dropped after re-exec (queue is in-memory).
- **[P3] CI uses `--new-from-rev=origin/main` for golangci-lint** — `.github/workflows/ci.yml:64`. Only new issues fail; pre-existing accumulate.
- **[P3] `make lint` silently no-ops if golangci-lint not installed** — `Makefile:85-90`. Quality gate "lint" runs `make lint` → silent pass.
- **[P3] `make release` requires manual `V=` and `NOTES=`** — parallel to autopilot's auto-release; drift risk.
- **[P3] Hot upgrade does not flush executor in-flight subprocess explicitly** — Claude Code subprocess becomes zombie under init.
- **[P3] No per-repo throttling for pollers** — busy repo starves others.
- **[P3] `autopilot.failure_reset_timeout=30m` not validated** — `0` silently means "never reset".
- **[P3] `autopilot.ci_wait_timeout=30m` not validated against `dev_ci_timeout=5m` semantics** — overlap with per-env `CITimeout`; precedence unclear.
- **[P4] `DefaultExecutionConfig.Mode = "auto"` but `Validate` requires lowercase enum** — `"Auto"` fails validation.
- **[P4] `DefaultProject` matching is case-insensitive but `Validate` is case-sensitive** — possible mismatch.
- **[P4] `DailyBrief.Time` deprecation only logs once via `log.Printf`** — bypasses structured logging.
- **[P4] `MemoryConfig.CrossProject` defaults to `true`** — surprising for multi-team setups.
- **[P4] `DefaultBackendConfig.AutoCreatePR=true`** — PR creation is default; surprise on first run outside autopilot.

---

## 4. Recurring themes

Patterns that crossed multiple streams. These are higher-leverage than individual findings.

### Theme A: Defensive coding instinct favors silent failure

- Streams: 5 (silent skip), 6 (silent disable bug class), 8 (silent env expansion, silent config clamp, silent lint pass), 7 (silent CLI deprecation)
- New gates default to silent `continue`. Env vars default to "use empty string if missing." Config values default to clamping silently. Quality gates default to passing if tooling absent. CLI flag deprecations default to silent alias.
- **Cumulative effect**: Operators see "Pilot stopped" or "the metric is flat" or "this feature doesn't work" without any signal. Average incident-resolution time is high because root cause requires source reading.

### Theme B: Default-off operational hardening

- Streams: 8 (retry, stagnation, subprocess limits, autopilot, alerts, daily-brief, quality, budget all default off), 5 (no panic recovery, no correlation IDs)
- Pilot ships with most hardening disabled. New users get the un-hardened experience; existing users may not realize hardening exists.
- **Cumulative effect**: The 2026-05-21 OOMs, dispatch loss, and quality flakes all happened on configurations that did not have the relevant hardening enabled, even though it shipped.

### Theme C: Triplication at adapter layer

- Streams: 1 (CS-6 adapter coupling), 2 (3 pollers ~90% identical, 7 `*_processed` tables, 6 messengers, 3 cleanup files)
- Each new ticket-system adapter requires ~1,000-3,000 LOC of near-duplicate code. Fixes in github don't propagate to gitlab/azuredevops.
- **Cumulative effect**: 2026-05-21 dispatch fix exists in github poller but not the other two; security-of-the-week (HMAC verification) varies by adapter.

### Theme D: Tests reproduce scenarios, not assert invariants

- Streams: 3 (no `t.Parallel`, no benchmarks, no fuzz, no `TestMain`), 6 (GH-201 needed 2 PRs to add 4 cases for the same predicate)
- The test surface enumerates known incidents. It does not assert "every caller of `CreateSubIssues` validates parent state" or "post-flight notifier and pre-dispatch poller agree on completion."
- **Cumulative effect**: Same bug class keeps regressing. Each fix adds a test for the specific path; the structural invariant remains unstated.

### Theme E: Documentation is prose, not derived

- Streams: 7 (version drift, feature counts, project structure all hand-maintained and wrong)
- Only `docs/lib/version.ts` is auto-synced. Everything else (CLAUDE.md "Current Status", FEATURE-MATRIX, ARCHITECTURE.md, inline `pilot v2.102.2` examples) drifts.
- **Cumulative effect**: Auto-loaded context lies. Memory citations point at fixed code. Onboarding docs teach hidden flags.

### Theme F: Configuration knobs that don't do what the doc says

- Streams: 8 (model routing all-same, releaser frozen at startup, `DefaultEnvironment` never read, `Config.Reload` dead code), 7 (`--autopilot` documented but deprecated)
- Several config fields look meaningful but are inert or only partially wired.
- **Cumulative effect**: Operators tune knobs that have no effect; misdiagnose system behavior.

---

## 5. Skipped scope and limitations

- **Defensive code review stream (Stream 4)** was dropped after 3 attempts triggered the LLM provider's usage-policy filter on security framing. Coverage of that surface is partial:
  - Webhook signature verification gaps: caught by Stream 3 (Linear + Azure DevOps missing HMAC).
  - Token logging hygiene: partial — Stream 5 caught `slog.Warn` patterns but did not exhaustively grep for `Printf("token=%s", token)` style leaks.
  - Repo allowlist bypass logging: caught by Stream 5 (P1 — no counter/alert).
  - Repo allowlist path semantics: caught by Stream 6 (P2 — no symlink/case normalization).
  - Test token hygiene: caught by Stream 3 (P3 — 30 sites using bespoke literals).
  - **Not covered**: shell-injection in `exec.Command(..., "sh", "-c", fmt.Sprintf(...))` patterns; path-traversal on caller-controlled filesystem paths; CORS on gateway HTTP handlers; SQL parameterization audit; approval-flow ID replay; subprocess arg-separation review.
- **Packages not audited**: `internal/budget`, `internal/replay`, `internal/transcription` (partial — Stream 3 noted 0 tests), `internal/upgrade` (partial — Stream 8 noted hot-upgrade gaps).
- **No code was modified** — this is research output only.
- **No GitHub issues or Navigator tasks were auto-created** — synthesis directs you to the appropriate next step in Section 2.
- **Each agent's coverage is bounded** by the model's read window. Long files (poller.go 1727 lines, runner.go 4556 lines) were sampled at offsets rather than fully read by some agents; the architecture and tech-debt streams explicitly re-read at multiple offsets.

---

## Appendix A: Architecture map

```
cmd/pilot (main, commands, handlers, repo_allowlist)
    │
    ├──► internal/pilot          ──► all adapters, executor, memory, gateway, etc.
    │       │ (god object, 35 fields)
    │       └──► internal/orchestrator ──► adapters/github, gitlab, jira, linear, asana, plane, executor
    │
    ├──► internal/executor       ──► memory, quality, replay, webhooks, logging
    │       │ (4556-line runner)
    │       │   ← internal/adapters/github/poller.go [COUPLING] ──► executor
    │       │   ← internal/adapters/linear/client.go [COUPLING] ──► executor
    │       │   ← internal/adapters/telegram/handler.go          ──► executor
    │       └──► (no adapter imports — clean)
    │
    ├──► internal/autopilot      ──► adapters/github [CONCRETE ↓], adapters/telegram [CONCRETE ↓]
    │       │                        memory, approval
    │       └── state_store.go   ──► sqlite (direct, owns 10 tables incl. 6 adapter tables)
    │
    ├──► internal/adapters/
    │       ├── github/          ──► executor [COUPLING ↑], logging, text
    │       ├── telegram/        ──► executor, comms, memory, briefs, transcription
    │       ├── linear/          ──► executor [for SubIssueCreator]
    │       ├── gitlab/          ──► memory (cleanup)
    │       ├── slack/           ──► comms
    │       ├── jira, asana, azuredevops, plane, discord/  ──► memory (cleanup), comms
    │       └── registry.go      (Adapter interface — used by jira, not by orchestrator/pilot)
    │
    ├──► internal/config         ──► ALL adapters + autopilot + executor + quality + ... [FAT HUB]
    │
    ├──► internal/alerts         ──► executor (adapter.go), adapters/slack, adapters/telegram [CONCRETE ↓]
    │
    ├──► internal/memory         ──► sqlite (primary, 18 tables)
    ├──► internal/gateway        ──► adapters/github (VerifyWebhookSignature), memory
    ├──► internal/comms          ──► executor, memory, intent, text
    ├──► internal/approval       ──► memory, logging
    ├──► internal/briefs         ──► memory, adapters/slack
    ├──► internal/wiring         ──► config, executor, autopilot, memory, e2e/mocks [TEST]
    └──► internal/{quality,replay,webhooks,budget,intent,logging,text,teams,...}  ──► leaf packages

Legend:
  [CONCRETE ↓]  = imports concrete adapter type, should be interface
  [COUPLING ↑]  = adapter imports executor (near-cycle risk)
  [FAT HUB]    = imports ≥10 packages
```

---

## Appendix B: Fix-flux table

Files most-touched by `fix:`/`revert:` commits in last 150 commits (excluding wiring):

| File | Fix commits | Hypothesis |
|---|---|---|
| `cmd/pilot/main.go` | 22 | Startup/wiring bind-point between every subsystem; no integration test for startup state machine. |
| `internal/executor/runner.go` | 21 | God-object orchestrator; length and branching are now the primary regression risk. |
| `internal/autopilot/controller.go` | 19 (+16 test) | Multiple skip gates; every fix discovers a new ordering. See `b140e441` hoisting recorder before gates that `c54dc79b` placed after. |
| `internal/executor/epic.go` (+12 test) | 12 | Decomposition + parent-state gating. Repeated GH-201 fixes. `isParentDone` is the entire correctness perimeter for cascade-loop class. |
| `internal/dashboard/tui.go` | 11 | Mostly UI cosmetics; lower severity. |
| `internal/memory/store.go` | 10 | Persistence semantics drift from in-code "completed" semantics. |
| `internal/adapters/github/poller.go` | 9 | Dispatch loop. Three+ silent-skip incidents (GH-2242, GH-2301, TASK-288). |

---

## Appendix C: Env var registry

| Var | Default | Where read | Documented? |
|---|---|---|---|
| `PILOT_ALLOW_UNMANAGED_REPO` | unset | `internal/executor/repo_guardrail.go:34`, `internal/adapters/github/issue_create.go:17` | godoc only |
| `PILOT_RESTARTED` | unset | `cmd/pilot/main.go:198,2707`; set by `internal/upgrade/restart.go:44` | godoc only |
| `PILOT_PREVIOUS_VERSION` | unset | `cmd/pilot/main.go:2708`; set by `internal/upgrade/restart.go:48` | godoc only |
| `PILOT_ENGINE_API_KEY` | unset | `backend_openai.go:58`, `backend_anthropic.go:58`, `preflight.go:176` | inline comments |
| `PILOT_CLASSIFIER_API_KEY` | unset | `effort_classifier.go:91` | inline comments |
| `PILOT_EXECUTOR=1` | always set (marker) | `backend_claudecode.go:388` | undocumented |
| `ANTHROPIC_API_KEY` | unset | many | yes (CLAUDE.md) |
| `ANTHROPIC_AUTH_TOKEN` | unset | many | godoc only |
| `CLAUDE_CODE_OAUTH_TOKEN` | unset | many | godoc only |
| `OPENAI_API_KEY` | unset | `setup.go:367`, `backend_openai.go` | yes |
| `GITHUB_TOKEN` | unset | 6 cmd sites + main.go | yes (CLAUDE.md) |
| `LINEAR_API_KEY` | unset | `onboard_ticket.go:276` | yes (CLAUDE.md, but overstated as required) |
| `SLACK_BOT_TOKEN` | unset | `onboard_notify.go:69` | yes (CLAUDE.md, but overstated as required) |
| `EDITOR`, `VISUAL` | unset | `config_cmd.go:100-102` | standard |
| `HOME` | tested | `tunnel/cloudflare_test.go` | standard |

No central env registry doc; no `pilot env` command; bypass vars not surfaced in `--help`.

---

## Appendix D: Coverage map

**Packages with 0 tests:**
| Package | Src LOC | Notes |
|---|---|---|
| `internal/banner` | 267 | Cosmetic UI; low risk |
| `internal/transcription` | 340 | Whisper API for Telegram voice — silent failure mode |
| `internal/testutil` | 93 | Test utility, exempt |

**Packages with <30% ratio:**
| Package | Ratio | Notes |
|---|---|---|
| `internal/pilot` | 7.8% | Contains `pilot.go` (1500 LOC main glue); only `webhook_handlers_test.go` covers a sliver |

**Critical source files lacking sibling test:**
| File | LOC | Risk |
|---|---|---|
| `internal/executor/backend_anthropic.go` | 784 | Direct Anthropic API backend — no test at all |
| `internal/executor/parallel.go` | 374 | Subagent fan-out, mutex/wg coordination |
| `internal/executor/runner_decompose.go` | 232 | Epic decomposition path |
| `internal/executor/markers.go` | 227 | Output marker parsing |
| `internal/executor/diagnose.go` | 175 | Failure diagnosis |
| `internal/executor/lint.go` | 140 | Post-execution lint |
| `internal/autopilot/metrics_persister.go` | 111 | DB writes for metrics |
| `internal/pilot/pilot.go` | 1,500 | Main entry glue |

**Critical paths with thin sibling tests:**
| File | Src | Test | Ratio |
|---|---|---|---|
| `internal/executor/backend.go` | 905 | 246 | 27% |
| `internal/executor/progress.go` | 565 | 180 | 31% |
| `internal/executor/gate.go` | 232 | 96 | 41% |

---

*Audit generated 2026-05-25 by parallel research synthesis. Source agents listed in `git log` near this file's commit.*
