# GH-2175

**Created:** 2026-04-18

## Problem

GitHub Issue #2175: Migrate all alekspetrov/pilot references to ylcn91/pilot

## Context

Repo migrated from `alekspetrov/pilot` to `ylcn91/pilot`. `go.mod` is correct, but an audit on 2026-04-18 found **~21 stale refs across 14 files**, including **functional bugs** — not just cosmetic leftovers.

## Scope — classified

### Must fix (functional)

| File | Why |
|---|---|
| `internal/gateway/dashboard.go:346` | Constructs broken issue URLs in dashboard (`anthropics/pilot`) |
| `internal/gateway/dashboard_test.go:584` | Test asserts the buggy URL — update in lockstep |
| `docs/content/deployment/azure.mdx:30` | `ghcr.io/anthropics/pilot` — wrong registry (Makefile pushes to `qf-studio`) |
| `docs/content/deployment/kubernetes.mdx:74,240` | Same |
| `docs/content/deployment/docker.mdx:80,93,167` | Same |
| `docs/content/deployment/docker-helm.mdx` (lines 8, 52, 55, 74, 142, 263, 816) | Same — 8 hits |
| `docs/content/deployment/aws.mdx:139` | Same |
| `docs/content/getting-started/quickstart.mdx:278` | Same |
| `pilot-bench/submit.sh:117` | Wrong URL in emitted leaderboard-submission JSON |

Docs auto-deploy to `pilot.quantflow.studio` via GitLab CI, so fixes ship to users on merge. Users copying `docker pull ghcr.io/anthropics/pilot:latest` hit 404.

### Should fix (misleading, non-functional)

| File | Why |
|---|---|
| `CLAUDE.md:148` | Import example uses `github.com/anthropics/pilot/...`; should match `go.mod` |
| `internal/executor/epic_test.go:972` | Parser fixture — update for consistency |
| `internal/autopilot/types_test.go:139-141` | "same-repo PR URL" fixture — update both `prURL` and `wantOwner` so the test name is truthful |

### Leave alone (historical)

- `pilot-bench/WORKLOG.md:156-157` — historical changelog entries linking to issues under the old org. Snapshot is accurate. Rewriting damages provenance.

## Execution

Mechanical find-and-replace with lockstep test updates. Use `no-decompose`.

- `github.com/anthropics/pilot` → `github.com/ylcn91/pilot` (code + docs)
- `ghcr.io/anthropics/pilot` → `ghcr.io/ylcn91/pilot` (docs)
- `github.com/alekspetrov/pilot` → `github.com/ylcn91/pilot` (submit.sh)
- `internal/autopilot/types_test.go`: update `prURL` AND `wantOwner` (the test name is "same-repo PR URL")
- **Skip** `pilot-bench/WORKLOG.md`

## Acceptance

```bash
rg 'alekspetrov/pilot|anthropics/pilot' \
  --glob '!pilot-bench/WORKLOG.md' \
  --glob '!.git/**'
# → no output

go build ./...
go test ./internal/gateway/... ./internal/autopilot/... ./internal/executor/...
```

## Related

- Supersedes the original narrower scope of this issue (3 files named).
- Unblocks anyone following the deployment guides.


## Acceptance Criteria

